<manifest xmlns:android="http://schemas.android.com/apk/res/android">

  <application>
    <activity
      android:name="com.livewire.sessions.SessionsActivity"
      android:exported="false"
      android:theme="@android:style/Theme.Material.Light.NoActionBar" />

    <provider
      android:name="com.livewire.sessions.SessionExportProvider"
      android:authorities="${applicationId}.livewire.sessions.exports"
      android:exported="false"
      android:grantUriPermissions="true" />
  </application>
</manifest>
